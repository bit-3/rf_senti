this component will responsible for handling
    #read the api d fetch tables -> get the routes
    #fetch each data source in pararell


dfetchTableConfig :
    # create a config for the requested endpoint.
    # batch it or single fetch the resource.



    todo : you have organize the data that comes from server as input
        NOT FOR TRANING FOR USE AND TEST


collecting in all mode you can not put the data on the webserver as http response.
